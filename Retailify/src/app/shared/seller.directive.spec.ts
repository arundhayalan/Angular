import { SellerDirective } from './seller.directive';

describe('SellerDirective', () => {
  it('should create an instance', () => {
    const directive = new SellerDirective();
    expect(directive).toBeTruthy();
  });
});
